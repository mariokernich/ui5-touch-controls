<core:FragmentDefinition
	xmlns="sap.m"
	xmlns:core="sap.ui.core"
	xmlns:f="sap.f"
	xmlns:cards="sap.f.cards"
	xmlns:editor="sap.ui.codeeditor">
	<!-- The snippets come from the "example" model instead of being written
	     into the view: curly braces in an XML view would be parsed as binding
	     syntax, so binding examples would swallow half of the code. -->
	<VBox width="100%" items="{example>/main}">
		<f:Card>
			<f:header>
				<cards:Header title="{example>title}" />
			</f:header>
			<f:content>
				<editor:CodeEditor
					value="{example>code}"
					type="{example>language}"
					height="{example>height}"
					width="100%"
					editable="false"
					lineNumbers="true"
					class="sapUiSmallMarginBegin sapUiSmallMarginEnd sapUiSmallMarginBottom" />
			</f:content>
		</f:Card>
	</VBox>
</core:FragmentDefinition>
